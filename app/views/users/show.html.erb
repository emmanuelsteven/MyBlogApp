<h1>Blog App</h1>

<%= render partial: 'partials/user', locals: { user: @user } %>

<div class='bio'><%=@user.bio%></div>
<ul class='user-post-list'>
<%if @user.three_recent_posts.empty?%>
<p class='no-posts'>No posts</p>
<%else%>
<%@user.three_recent_posts.each do |post| %>
<li>
<%= render partial: 'partials/post',locals: { post: post } %>
</li>
<%end%>
<%end%>
</ul>
<div class='content-view-all-post-btn'>
<%= link_to "View all posts", user_posts_path(@user), class: 'view-all-post-btn' %></div>